{
  "name": "whyfull",
  "version": "0.0.1",
  "private": false,
  "description": "Find where disk space went on a dev machine (AI caches, package stores, build output), ranked by how safe each is to delete. Read-only.",
  "keywords": [
    "disk",
    "disk-space",
    "cache",
    "cleanup",
    "huggingface",
    "ollama",
    "lmstudio",
    "docker",
    "node_modules",
    "devtools"
  ],
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "whyfull": "dist/cli.mjs"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18.15"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.mjs\"",
    "dev": "tsdown --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "mri": "^1.2.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@yful/tsconfig": "workspace:*",
    "tsdown": "^0.22.14",
    "typescript": "^5.7.2"
  }
}
